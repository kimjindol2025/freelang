// stdlib/regex - 정규표현식 라이브러리
// 주: 정규식 함수는 인터프리터 수준에서 직접 구현됨
// 라이브러리 파일은 문서화 목적으로만 사용

// 사용 예:
// IMPORT regex FROM "stdlib/regex"
// SET has_match = regex.test("[0-9]+", "abc123")
// SET matches = regex.matchAll("[0-9]+", "abc123def456")
// SET replaced = regex.replace("[0-9]+", "abc123", "X")
